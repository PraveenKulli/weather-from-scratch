# Weather Backend (Node + Express + SQLite + JWT)

## Setup
```bash
cp .env.example .env
# Fill OWM_KEYS with 5 comma-separated API keys
npm install
npm run seed
npm start
```
API runs at `http://localhost:4000`
